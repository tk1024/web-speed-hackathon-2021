import { ROOT_PATH } from '../../root';

const PUBLIC_PATH = ROOT_PATH + '/public';
const UPLOAD_PATH = ROOT_PATH + '/public';
const CLIENT_DIST_PATH = ROOT_PATH + '/dist';
const DATABASE_PATH = ROOT_PATH + '/database.sqlite';

export { PUBLIC_PATH, CLIENT_DIST_PATH, DATABASE_PATH, UPLOAD_PATH };